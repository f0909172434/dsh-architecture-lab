diff --git a/src/runner.ts b/src/runner.ts
index bdbd0be..1a2414f 100644
--- a/src/runner.ts
+++ b/src/runner.ts
@@ -1,5 +1,5 @@
 import { spawn, spawnSync } from 'node:child_process'
-import { mkdir, readdir, readFile, rm, stat, writeFile } from 'node:fs/promises'
+import { cp, mkdir, readdir, readFile, rm, stat, writeFile } from 'node:fs/promises'
 import { createRequire } from 'node:module'
 import { join, resolve } from 'node:path'
 import { checkAssertions } from './assert.js'
@@ -43,6 +43,16 @@ export interface RunOptions {
   tags?: string[]
   /** 只跑这些名字（精确匹配）的用例 */
   only?: string[]
+  /** Copy a frozen task fixture into every freshly reset attempt workspace. */
+  seedDir?: string
+  /** Frozen Engram DB snapshot copied into a unique directory for each attempt. */
+  memorySnapshotDir?: string
+  /** Stop the active child and do not schedule further cases or attempts. */
+  signal?: AbortSignal
+  /** Trusted embedding host may supply a pre-resolved executable. */
+  command?: DshCommand
+  /** Public orchestration seam: parent owns containment, transport and billing. */
+  processRunner?: (input: { prompt: string; workspace: string; sessionRoot: string; timeoutMs: number; signal?: AbortSignal }) => Promise<{ code: number | null; timedOut: boolean; stderrTail: string }>
 }
 
 const PREFIX = 'eval_run'
@@ -249,13 +259,20 @@ function slugify(name: string): string {
  * 多帧 zstd（session.jsonl.zstd），见 collector.collectFromFile。
  * root 用 JSON.stringify 转义为 YAML 双引号标量；name 含 @ 必须单引号包裹。
  */
-export function buildOverlayYaml(root: string): string {
+export function buildOverlayYaml(root: string, memoryDir?: string): string {
   return [
     '# generated by dsh-eval-harness eval_run — overlay for --patch (highest layer priority)',
     '- id: session-persistence-jsonl',
     "  name: '@deepseek-ai/dsh-session-persistence-jsonl'",
     '  config:',
     `    root: ${JSON.stringify(root)}`,
+    ...(memoryDir === undefined ? [] : [
+      '- id: dsh-engram',
+      '  config:',
+      `    dbDir: ${JSON.stringify(memoryDir)}`,
+      "    ingest: 'off'",
+      "    legacyMigration: 'conservative'",
+    ]),
     '',
   ].join('\n')
 }
@@ -287,7 +304,7 @@ export async function findSessionFile(dir: string, sinceMs: number): Promise<str
       const p = join(d, entry.name)
       if (entry.isDirectory()) {
         found.push(...(await walk(p)))
-      } else if (entry.name === 'session.jsonl' || entry.name === 'session.jsonl.zstd') {
+      } else if (entry.name === 'session.jsonl' || entry.name === 'session.jsonl.zstd' || entry.name === 'session.v3.jsonl.zstd') {
         const mtime = (await stat(p)).mtimeMs
         if (mtime >= sinceMs) found.push({ path: p, mtime })
       }
@@ -317,6 +334,7 @@ function runOne(
   args: string[],
   cwd: string,
   timeoutMs: number,
+  signal?: AbortSignal,
 ): Promise<{ code: number | null; timedOut: boolean; stderrTail: string }> {
   return new Promise((resolvePromise, reject) => {
     const child = spawn(bin, args, {
@@ -324,6 +342,9 @@ function runOne(
       env: { ...process.env },
       stdio: ['ignore', 'ignore', 'pipe'],
     })
+    const onAbort = () => child.kill('SIGKILL')
+    signal?.addEventListener('abort', onAbort, { once: true })
+    if (signal?.aborted) onAbort()
     let stderr = ''
     let killedByTimeout = false
     child.stderr.on('data', (chunk: Buffer) => {
@@ -336,10 +357,12 @@ function runOne(
     }, timeoutMs)
     child.on('error', (err) => {
       clearTimeout(timer)
+      signal?.removeEventListener('abort', onAbort)
       reject(new Error(`${PREFIX}: failed to spawn dsh: ${err.message}`))
     })
     child.on('close', (code) => {
       clearTimeout(timer)
+      signal?.removeEventListener('abort', onAbort)
       resolvePromise({ code, timedOut: killedByTimeout, stderrTail: stderr.trim() })
     })
   })
@@ -413,7 +436,7 @@ export async function runEval(options: RunOptions): Promise<RunReport> {
   const sessionBase = resolve(options.sessionRoot ?? join(outputDir, '.sessions'))
   const workspaceBase = join(outputDir, '.workspace')
 
-  const dsh = resolveDshCommand(options.dshBin)
+  const dsh = options.command ?? resolveDshCommand(options.dshBin)
   const allCases = await loadCases(casesDir)
   // 带原始加载序号过滤：隔离目录用加载序（而非筛选后的运行序），
   // 同一条用例全量跑 / only 单跑时 artifact 路径稳定
@@ -449,10 +472,19 @@ export async function runEval(options: RunOptions): Promise<RunReport> {
       // 时间窗边界）；sinceMs 保留为第二道防线，但隔离不再依赖它
       await rm(workspace, { recursive: true, force: true })
       await mkdir(workspace, { recursive: true })
+      if (options.seedDir !== undefined) {
+        await cp(resolve(options.seedDir), workspace, { recursive: true, force: true })
+      }
       const sessionRoot = join(sessionBase, dirName, `attempt-${String(attemptIndex)}`)
+      const memoryDir = options.memorySnapshotDir === undefined ? undefined : join(outputDir, '.engram', dirName, `attempt-${String(attemptIndex)}`)
+      if (memoryDir !== undefined) {
+        await rm(memoryDir, { recursive: true, force: true })
+        await mkdir(memoryDir, { recursive: true })
+        await cp(resolve(options.memorySnapshotDir!), memoryDir, { recursive: true, force: true })
+      }
       const overlayPath = join(outputDir, `eval-overlay-${dirName}-a${String(attemptIndex)}.patch.yml`)
       await mkdir(sessionRoot, { recursive: true })
-      await writeFile(overlayPath, buildOverlayYaml(sessionRoot))
+      await writeFile(overlayPath, buildOverlayYaml(sessionRoot, memoryDir))
       const startedAt = Date.now()
       const base: Omit<AttemptResult, 'index' | 'status' | 'durationMs'> = {
         failures: [],
@@ -467,8 +499,15 @@ export async function runEval(options: RunOptions): Promise<RunReport> {
         skippedLines: 0,
       }
       try {
-        const proc = await runOne(dsh.bin, [...dsh.prefixArgs, ...buildDshArgs(profile, overlayPath, evalCase.prompt)], workspace, timeoutMs)
+        if (options.signal?.aborted) throw new Error('experiment interrupted before launch')
+        const proc = options.processRunner
+          ? await options.processRunner({ prompt: evalCase.prompt, workspace, sessionRoot, timeoutMs, signal: options.signal })
+          : await runOne(dsh.bin, [...dsh.prefixArgs, ...buildDshArgs(profile, overlayPath, evalCase.prompt)], workspace, timeoutMs, options.signal)
         const procFields = processFields(proc)
+        if (options.signal?.aborted) {
+          const partial = await tryCollectTrace(sessionRoot, startedAt)
+          return { ...base, ...(partial ? traceFields(partial) : {}), ...procFields, status: 'error', error: 'experiment interrupted', durationMs: Date.now() - startedAt }
+        }
         if (proc.timedOut) {
           const partial = await tryCollectTrace(sessionRoot, startedAt)
           return {
@@ -550,7 +589,7 @@ export async function runEval(options: RunOptions): Promise<RunReport> {
     let result = await runAttempt(1)
     attemptResults.push({ ...result, index: 1 })
     if (trials > 1) {
-      while (attemptResults.length < trials) {
+      while (attemptResults.length < trials && !options.signal?.aborted) {
         result = await runAttempt(attemptResults.length + 1)
         attemptResults.push({ ...result, index: attemptResults.length + 1 })
       }
@@ -571,7 +610,7 @@ export async function runEval(options: RunOptions): Promise<RunReport> {
         durationMs: Date.now() - totalStartedAt,
       }
     }
-    while (result.status !== 'pass' && attemptResults.length <= retries) {
+    while (result.status !== 'pass' && attemptResults.length <= retries && !options.signal?.aborted) {
       result = await runAttempt(attemptResults.length + 1)
       attemptResults.push({ ...result, index: attemptResults.length + 1 })
     }
@@ -591,7 +630,7 @@ export async function runEval(options: RunOptions): Promise<RunReport> {
   const results: (CaseResult | undefined)[] = new Array(cases.length)
   let next = 0
   const worker = async (): Promise<void> => {
-    while (next < cases.length) {
+    while (next < cases.length && !options.signal?.aborted) {
       const i = next++
       const item = cases[i]
       if (!item) break
@@ -599,6 +638,29 @@ export async function runEval(options: RunOptions): Promise<RunReport> {
     }
   }
   await Promise.all(Array.from({ length: Math.min(concurrency, cases.length) }, () => worker()))
+  if (options.signal?.aborted) {
+    for (let i = 0; i < cases.length; i++) {
+      if (results[i] !== undefined) continue
+      results[i] = {
+        name: cases[i]!.evalCase.name,
+        status: 'error',
+        error: 'experiment interrupted before case launch',
+        durationMs: 0,
+        attempts: 0,
+        attemptResults: [],
+        failures: [],
+        toolsCalled: [],
+        toolCalls: [],
+        toolResults: [],
+        finalText: '',
+        steps: 0,
+        tokens: emptyTokenUsage(),
+        toolErrors: [],
+        events: 0,
+        skippedLines: 0,
+      }
+    }
+  }
 
   const runFinishedAtMs = Date.now()
   const report: RunReport = {
diff --git a/tests/runner.spec.ts b/tests/runner.spec.ts
index 07bd614..5317306 100644
--- a/tests/runner.spec.ts
+++ b/tests/runner.spec.ts
@@ -9,6 +9,11 @@ const sessionJsonl = (depth: number, extra = ''): string =>
   `{"type":"session","version":0,"id":"s","createdAt":1,"cwd":"/x","delegationDepth":${depth}}\n${extra}`
 
 describe('buildOverlayYaml', () => {
+  it('pins an attempt to its copied Engram database and disables ingestion', () => {
+    const yaml = buildOverlayYaml('/tmp/sessions', '/tmp/engram-attempt')
+    expect(yaml).toContain('dbDir: "/tmp/engram-attempt"')
+    expect(yaml).toContain("ingest: 'off'")
+  })
   it('overrides session-persistence-jsonl row with isolated root (leaving default zstd compression)', () => {
     const yaml = buildOverlayYaml('/tmp/eval-out/.sessions')
     expect(yaml).toBe(
@@ -37,6 +42,72 @@ describe('buildOverlayYaml', () => {
   })
 })
 
+describe('lab fixture and cancellation integration', () => {
+  it('uses an injected contained runner without probing or spawning the default executable', async () => {
+    const root = await mkdtemp(join(tmpdir(), 'eval-contained-'))
+    const casesDir = join(root, 'cases'), seedDir = join(root, 'seed')
+    await mkdir(casesDir); await mkdir(seedDir)
+    await writeFile(join(seedDir, 'index.mjs'), 'fixture')
+    await writeFile(join(casesDir, 'one.yml'), 'name: contained\nprompt: "fixed prompt"\nassert:\n  tools_called: [bash]\n')
+    let launches = 0
+    const report = await runEval({
+      casesDir, outputDir: join(root, 'out'), seedDir,
+      command: { bin: '/must/not/execute', prefixArgs: [], version: 'isolated-test' },
+      processRunner: async ({ prompt, workspace, sessionRoot }) => {
+        launches++
+        expect(prompt).toBe('fixed prompt')
+        expect(await readFile(join(workspace, 'index.mjs'), 'utf8')).toBe('fixture')
+        await writeFile(join(sessionRoot, 'session.jsonl'), sessionJsonl(0, '{"type":"tool/call","seq":1,"time":1,"data":{"turn":1,"step":1,"callId":"c1","name":"bash","arguments":"{}"}}\n'))
+        return { code: 0, timedOut: false, stderrTail: '' }
+      },
+    })
+    expect(launches).toBe(1)
+    expect(report.dshVersion).toBe('isolated-test')
+    expect(report.cases[0]?.status).toBe('pass')
+    expect(JSON.parse(await readFile(join(root,'out/report.json'),'utf8')).cases[0].status).toBe('pass')
+  })
+  it('rebuilds task and memory fixtures before launching the fake agent', async () => {
+    const root = await mkdtemp(join(tmpdir(), 'eval-lab-seed-'))
+    const casesDir = join(root, 'cases')
+    const seedDir = join(root, 'seed')
+    const memorySnapshotDir = join(root, 'memory')
+    await Promise.all([mkdir(casesDir), mkdir(seedDir), mkdir(memorySnapshotDir)])
+    await writeFile(join(casesDir, 'one.yml'), 'name: fixture\nprompt: "p"\nassert:\n  tools_called: [bash]\n')
+    await writeFile(join(seedDir, 'index.mjs'), 'export const value = 1\n')
+    await writeFile(join(memorySnapshotDir, 'user.db'), 'fixed snapshot')
+    const fakeBin = join(root, 'fake-dsh')
+    await writeFile(fakeBin, [
+      '#!/bin/sh',
+      'if [ "$1" = "--version" ]; then echo 0.0.0-fake; exit 0; fi',
+      'test -f index.mjs || exit 3',
+      'db=$(sed -n \x27s/^    dbDir: "\\(.*\\)"$/\\1/p\x27 "$4")',
+      'test "$(cat "$db/user.db")" = "fixed snapshot" || exit 4',
+      'root=$(sed -n \x27s/^    root: "\\(.*\\)"$/\\1/p\x27 "$4")',
+      'dir="$root/case/session-fake"',
+      'mkdir -p "$dir"',
+      'printf \x27%s\\n\x27 \x27{"type":"session","version":0,"id":"s","createdAt":1,"cwd":"/x","delegationDepth":0}\x27 \x27{"type":"tool/call","seq":1,"time":1,"data":{"turn":1,"step":1,"callId":"c1","name":"bash","arguments":"{}"}}\x27 > "$dir/session.jsonl"',
+      '',
+    ].join('\n'), { mode: 0o755 })
+    const report = await runEval({ casesDir, outputDir: join(root, 'out'), seedDir, memorySnapshotDir, dshBin: fakeBin })
+    expect(report.cases[0]?.status).toBe('pass')
+  })
+
+  it('stops a running child and records unstarted cases as interrupted errors', async () => {
+    const root = await mkdtemp(join(tmpdir(), 'eval-lab-abort-'))
+    const casesDir = join(root, 'cases')
+    await mkdir(casesDir)
+    for (const [i, name] of ['first', 'second'].entries()) await writeFile(join(casesDir, `${i}.yml`), `name: ${name}\nprompt: "p"\nassert:\n  tools_called: [bash]\n`)
+    const fakeBin = join(root, 'fake-dsh')
+    await writeFile(fakeBin, '#!/usr/bin/env node\nif (process.argv[2] === "--version") { console.log("0.0.0-fake"); process.exit(0) }\nsetTimeout(() => {}, 10000)\n', { mode: 0o755 })
+    const controller = new AbortController()
+    setTimeout(() => controller.abort(), 200)
+    const report = await runEval({ casesDir, outputDir: join(root, 'out'), dshBin: fakeBin, signal: controller.signal, timeoutMs: 10_000 })
+    expect(report.cases).toHaveLength(2)
+    expect(report.cases.every((item) => item.status === 'error')).toBe(true)
+    expect(report.cases[1]?.error).toContain('interrupted before case launch')
+  }, 15_000)
+})
+
 describe('splitDshBin', () => {
   it('plain executable has no prefix args', () => {
     expect(splitDshBin('dsh')).toEqual({ bin: 'dsh', prefixArgs: [] })
