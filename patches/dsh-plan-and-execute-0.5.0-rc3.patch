diff --git a/package.json b/package.json
index bc65586..4de9f89 100644
--- a/package.json
+++ b/package.json
@@ -82,0 +83,3 @@
+    "@deepseek-ai/dsh-client-store": "0.1.5-rc.3",
+    "@deepseek-ai/dsh-client-ui-primitives": "0.1.5-rc.3",
+    "@deepseek-ai/dsh-client-ui-slots": "0.1.5-rc.3",
diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
index ab02bef..2fc9d27 100644
--- a/pnpm-lock.yaml
+++ b/pnpm-lock.yaml
@@ -14,0 +15,9 @@ importers:
+      '@deepseek-ai/dsh-client-store':
+        specifier: 0.1.5-rc.3
+        version: 0.1.5-rc.3
+      '@deepseek-ai/dsh-client-ui-primitives':
+        specifier: 0.1.5-rc.3
+        version: 0.1.5-rc.3
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: 0.1.5-rc.3
+        version: 0.1.5-rc.3
@@ -114,0 +124,15 @@ packages:
+  '@deepseek-ai/dsh-client-store@0.1.5-rc.3':
+    resolution: {integrity: sha512-tSLn0JSWhDtnXEjp+FPDMjadekyJFMSlaGtR1Fdjpd7EgVcDd4Hsw6gP/nG6Mo/4yOTEfLUSDNFp5AEzK2qs1Q==}
+    peerDependencies:
+      '@deepseek-ai/cordis': 4.0.2
+
+  '@deepseek-ai/dsh-client-ui-primitives@0.1.5-rc.3':
+    resolution: {integrity: sha512-PnPSAppguOfDdtGRVLOtVynoZml3676GO/tveRuaA+tKhXog/35+JIpPewe7QQ+jwbxl+S/p66R6P19az3Cglw==}
+    peerDependencies:
+      '@deepseek-ai/cordis': 4.0.2
+
+  '@deepseek-ai/dsh-client-ui-slots@0.1.5-rc.3':
+    resolution: {integrity: sha512-6mYci9acjKVuu7ZoEfakMKOdrMmRZvlohSRKI/Eu3xQCl1Z2HCpIKR9tUdNwA/3gliKtu1cxmnUi2Jqmm1Mmxg==}
+    peerDependencies:
+      '@deepseek-ai/cordis': 4.0.2
+
@@ -1703,0 +1728,6 @@ snapshots:
+  '@deepseek-ai/dsh-client-store@0.1.5-rc.3': {}
+
+  '@deepseek-ai/dsh-client-ui-primitives@0.1.5-rc.3': {}
+
+  '@deepseek-ai/dsh-client-ui-slots@0.1.5-rc.3': {}
+
diff --git a/src/index.ts b/src/index.ts
index 7178f19..1b65b5c 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -66 +66 @@ export const Config: Schema<Config> = Schema.object({
-function toDriveAgent(agent: Agent): DriveAgent {
+export function toDriveAgent(agent: Agent): DriveAgent {
@@ -70,2 +70,7 @@ function toDriveAgent(agent: Agent): DriveAgent {
-    events: session.snapshotEvents(),
-    surface: session.surface,
+    get events() { return session.snapshotEvents() },
+    get surface() {
+      // Host rc.3 requires the system head to survive user-context replacement.
+      const events = session.snapshotEvents()
+      const systemSeqs = new Set(events.filter(event => event.type === 'system/message').map(event => event.seq))
+      return { nodes: session.surface.nodes.filter(seq => !systemSeqs.has(seq)), replaceGeneration: session.surface.replaceGeneration }
+    },
@@ -82 +87 @@ function toDriveAgent(agent: Agent): DriveAgent {
-        surfaceOp: { op: 'replace', start: SessionSeq(start), end: SessionSeq(end) },
+        surfaceOp: { op: 'replace', startSeq: SessionSeq(start), endSeq: SessionSeq(end) },
@@ -344,2 +349,3 @@ export function apply(ctx: Context, config: Config): void {
-      input: { hint: '<任务描述>', images: true },
-      handler: async ({ agent, rawInput, attachments }) => {
+      input: { hint: '<任务描述>', attachments: true },
+      handler: async ({ agent, rawInput, attachments = [] }) => {
+        if (attachments.some((block) => block.type !== 'image')) return { kind: 'error', text: '此版本规划插件仅支持图片附件。' }
@@ -383 +389 @@ export function apply(ctx: Context, config: Config): void {
-        await orchestrator.begin(task, attachments)
+        await orchestrator.begin(task, attachments.filter((block) => block.type === 'image'))
diff --git a/src/orchestrator.ts b/src/orchestrator.ts
index 668613e..eea4c1a 100644
--- a/src/orchestrator.ts
+++ b/src/orchestrator.ts
@@ -323 +323 @@ export class Orchestrator {
-      void error
+      console.error("[dsh-plan-and-execute] orchestration failed", error)
diff --git a/test/host-adapter.spec.ts b/test/host-adapter.spec.ts
new file mode 100644
index 0000000..6ebd7ac
--- /dev/null
+++ b/test/host-adapter.spec.ts
@@ -0,0 +1,25 @@
+import { describe, expect, it } from 'vitest'
+import { Session, SessionId } from '@deepseek-ai/dsh-session'
+import { createSystemMessage } from '@deepseek-ai/dsh-llm'
+import { toDriveAgent } from '../src/index.ts'
+import { userTaskMessage } from '../src/prompts.ts'
+
+describe('Harness rc.3 public session adapter', () => {
+  it('sees appended events and replaces conversation context without deleting the system head', () => {
+    const session = Session.create(SessionId('adapter-regression'))
+    const drive = toDriveAgent({ session, steer() {}, async whenIdle() {} } as never).session
+    session.append('system/message', { turn: 0, step: 0, message: createSystemMessage('Preserve host rules.', 'test') }, { surfaceOp: 'append' })
+    expect(drive.surface.nodes).toEqual([])
+    const original = session.append('user/message', userTaskMessage('First task'), { surfaceOp: 'append' })
+    expect(drive.events.length).toBe(2)
+    expect(drive.surface.nodes).toEqual([original.seq])
+    const replacement = drive.replaceSurface(userTaskMessage('Step context'), original.seq, original.seq, [original.seq])
+    expect(session.surface.nodes).toEqual([0, replacement])
+    expect(drive.surface.nodes).toEqual([replacement])
+    expect(drive.events.length).toBe(3)
+    expect(session.snapshotEvents()[0]?.type).toBe('system/message')
+    const next = drive.replaceSurface(userTaskMessage('Next step'), replacement, replacement, [replacement])
+    expect(session.surface.nodes).toEqual([0, next])
+    expect(drive.events.length).toBe(4)
+  })
+})
diff --git a/test/index.spec.ts b/test/index.spec.ts
index d4fa4a0..a29237c 100644
--- a/test/index.spec.ts
+++ b/test/index.spec.ts
@@ -222 +222 @@ describe('apply 装配', () => {
-      { seq: 1, type: 'plan/mode', data: { active: true } } as SessionEvent,
+      { seq: 1, time: 0, type: 'plan/mode', data: { active: true } } as unknown as SessionEvent,
@@ -248 +248 @@ describe('apply 装配', () => {
-  it('命令声明 input.images: true；带图启动 → 锚定消息图块在前 + 落盘 taskImages', async () => {
+  it('命令声明 input.attachments: true；带图启动 → 锚定消息图块在前 + 落盘 taskImages', async () => {
@@ -253 +253 @@ describe('apply 装配', () => {
-    expect(definition.input).toEqual({ hint: '<任务描述>', images: true })
+    expect(definition.input).toEqual({ hint: '<任务描述>', attachments: true })
diff --git a/test/state.spec.ts b/test/state.spec.ts
index 3a1ea75..be2e4af 100644
--- a/test/state.spec.ts
+++ b/test/state.spec.ts
@@ -36,2 +36,2 @@ describe('isPlanModeActive', () => {
-      { seq: 1, type: 'plan/mode', data: { active: true } } as SessionEvent,
-      { seq: 2, type: 'plan/mode', data: { active: false } } as SessionEvent,
+      { seq: 1, time: 0, type: 'plan/mode', data: { active: true } } as unknown as SessionEvent,
+      { seq: 2, time: 0, type: 'plan/mode', data: { active: false } } as unknown as SessionEvent,
